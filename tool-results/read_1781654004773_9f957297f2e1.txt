     1→     1→import hashlib
     2→     2→import hmac
     3→     3→import logging
     4→     4→import time
     5→     5→from collections import deque
     6→     6→from typing import Any
     7→     7→
     8→     8→from fastapi import APIRouter, BackgroundTasks, Request, Response
     9→     9→
    10→    10→from app.config import settings
    11→    11→from app.services.ifood_api import IFoodAPIClient
    12→    12→from app.services.odoo_client import OdooClient
    13→    13→from app.services.odoo_sync import OdooSyncService
    14→    14→
    15→    15→logger = logging.getLogger(__name__)
    16→    16→router = APIRouter()
    17→    17→
    18→    18→# ── In-memory event log for debugging ─────────────────────────
    19→    19→_WEBHOOK_LOG: deque = deque(maxlen=100)
    20→    20→
    21→    21→
    22→    22→def _log_event(entry: dict) -> None:
    23→    23→    """Append event to in-memory log."""
    24→    24→    entry["_ts"] = time.time()
    25→    25→    _WEBHOOK_LOG.append(entry)
    26→    26→
    27→    27→
    28→    28→@router.get("/webhooks/ifood/debug-log")
    29→    29→async def get_webhook_log():
    30→    30→    """Retorna os ultimos eventos de webhook processados (para debug)."""
    31→    31→    return {"count": len(_WEBHOOK_LOG), "events": list(_WEBHOOK_LOG)}
    32→    32→
    33→    33→
    34→    34→# ── Signature validation ─────────────────────────────────────
    35→    35→
    36→    36→def _verify_webhook_signature(request: Request, body: bytes) -> bool:
    37→    37→    """Verifica a assinatura do webhook iFood.
    38→    38→
    39→    39→    iFood envia header 'x-ifood-signature-v1' com valor 'sha256=<hex>'.
    40→    40→    """
    41→    41→    if not settings.webhook_secret or settings.webhook_secret == "change-me-webhook-secret":
    42→    42→        logger.debug("WEBHOOK_SECRET nao configurado - pulando verificacao")
    43→    43→        return True
    44→    44→
    45→    45→    signature = (
    46→    46→        request.headers.get("x-ifood-signature-v1")
    47→    47→        or request.headers.get("x-ifood-signature")
    48→    48→        or request.headers.get("X-IFood-Signature")
    49→    49→        or ""
    50→    50→    )
    51→    51→
    52→    52→    if not signature:
    53→    53→        logger.info("Webhook recebido SEM assinatura - possivel teste de conexao")
    54→    54→        return True
    55→    55→
    56→    56→    # Calcular hash esperado
    57→    57→    expected_hash = hmac.new(
    58→    58→        settings.webhook_secret.encode("utf-8"),
    59→    59→        body,
    60→    60→        hashlib.sha256,
    61→    61→    ).hexdigest()
    62→    62→
    63→    63→    # iFood envia no formato "sha256=<hex>" - comparar o hash diretamente
    64→    64→    received_hash = signature
    65→    65→    if signature.startswith("sha256="):
    66→    66→        received_hash = signature[7:]  # remove "sha256=" prefix
    67→    67→
    68→    68→    if not hmac.compare_digest(expected_hash, received_hash):
    69→    69→        logger.warning(
    70→    70→            "Assinatura INVALIDA! Recebida: %s | Hash calculado: %s | Body len: %d",
    71→    71→            signature, expected_hash, len(body),
    72→    72→        )
    73→    73→    else:
    74→    74→        logger.debug("Assinatura do webhook valida")
    75→    75→
    76→    76→    # Sempre retorna True para nao rejeitar o iFood
    77→    77→    return True
    78→    78→
    79→    79→
    80→    80→# ── Main webhook handler ─────────────────────────────────────
    81→    81→
    82→    82→@router.post("/webhooks/ifood/orders")
    83→    83→@router.get("/webhooks/ifood/orders")
    84→    84→async def handle_order_webhook(request: Request, response: Response, background_tasks: BackgroundTasks):
    85→    85→    """Recebe eventos de pedidos do iFood.
    86→    86→
    87→    87→    Regra #1: SEMPRE retorna 200/202 para o iFood (nao retry).
    88→    88→    Regra #2: CANCELLATION_REQUESTED retorna 202 imediato e processa em background (iFood exige resp < 5s).
    89→    89→    Regra #3: Cada passo (auth, confirm, sync) e independente.
    90→    90→    Regra #4: Tudo e logado para debug.
    91→    91→
    92→    92→    Fluxo PLC (novo pedido):
    93→    93→      1. Buscar dados completos do pedido no iFood
    94→    94→      2. Confirmar pedido no iFood (POST /confirm)
    95→    95→      3. Sincronizar com Odoo
    96→    96→
    97→    97→    Fluxo CANCELLATION_REQUESTED (cliente solicita cancelamento):
    98→    98→      1. Responder 202 imediatamente
    99→    99→      2. Em background: aceitar cancelamento no iFood (POST /cancellation/accept)
   100→   100→      3. Aguardar evento CANCELLED para atualizar Odoo
   101→   101→
   102→   102→    Fluxo CANCELLED (cancelamento concluido):
   103→   103→      1. Atualizar status no Odoo para 'cancelled'
   104→   104→    """
   105→   105→    # Variaveis de contexto para logging
   106→   106→    event_type = "unknown"
   107→   107→    order_id = ""
   108→   108→
   109→   109→    try:
   110→   110→        body = await request.body()
   111→   111→        method = request.method
   112→   112→
   113→   113→        logger.info("=" * 60)
   114→   114→        logger.info("=== Webhook iFood recebido ===")
   115→   115→        logger.info("Method: %s | Content-Type: %s | Body len: %d",
   116→   116→                     method, request.headers.get("content-type", "N/A"), len(body))
   117→   117→        logger.debug("Headers: %s", dict(request.headers))
   118→   118→        logger.debug("Body RAW: %s", body.decode("utf-8", errors="replace")[:3000])
   119→   119→
   120→   120→        _verify_webhook_signature(request, body)
   121→   121→
   122→   122→        # GET = teste de conexao do iFood
   123→   123→        if method == "GET":
   124→   124→            logger.info("Teste de conexao (GET) - respondendo 200")
   125→   125→            return {"status": "ok", "message": "Webhook endpoint is alive"}
   126→   126→
   127→   127→        # Parsear payload
   128→   128→        try:
   129→   129→            payload = await request.json()
   130→   130→        except Exception as parse_err:
   131→   131→            logger.error("[FATAL] Nao foi possivel parsear JSON do webhook: %s", parse_err)
   132→   132→            logger.error("[FATAL] Body: %s", body.decode("utf-8", errors="replace")[:2000])
   133→   133→            _log_event({"level": "error", "message": "JSON parse failed", "error": str(parse_err)})
   134→   134→            return {"status": "ok", "message": "received"}  # sempre 200
   135→   135→
   136→   136→        event_type = (
   137→   137→            payload.get("code")
   138→   138→            or payload.get("eventType")
   139→   139→            or payload.get("event")
   140→   140→            or payload.get("type")
   141→   141→            or "unknown"
   142→   142→        )
   143→   143→        order_id = str(payload.get("orderId", "") or "")
   144→   144→        merchant_id = str(payload.get("merchantId", "") or "")
   145→   145→
   146→   146→        logger.info("Evento: %s | Pedido: %s | Merchant: %s", event_type, order_id, merchant_id)
   147→   147→
   148→   148→        _log_event({
   149→   149→            "level": "info",
   150→   150→            "event_type": event_type,
   151→   151→            "order_id": order_id,
   152→   152→            "merchant_id": merchant_id,
   153→   153→            "method": method,
   154→   154→        })
   155→   155→
   156→   156→        # ── KEEPALIVE: heartbeat do iFood ─────────────────────
   157→   157→        if event_type == "KEEPALIVE":
   158→   158→            logger.debug("[KEEPALIVE] Heartbeat recebido")
   159→   159→            # Aproveitar o heartbeat para verificar acoes pendentes no Odoo
   160→   160→            try:
   161→   161→                await _check_odoo_pending_cancellations()
   162→   162→            except Exception as poll_err:
   163→   163→                logger.warning("[KEEPALIVE] Falha ao verificar cancelamentos Odoo: %s", poll_err)
   164→   164→            try:
   165→   165→                await _check_odoo_pending_dispatches()
   166→   166→            except Exception as poll_err:
   167→   167→                logger.warning("[KEEPALIVE] Falha ao verificar despachos Odoo: %s", poll_err)
   168→   168→            return {"status": "ok", "eventType": "KEEPALIVE"}
   169→   169→
   170→   170→        # ── PLC: Novo Pedido ──────────────────────────────────
   171→   171→        if event_type in ("PLC", "NEW", "orderCreated", "placed"):
   172→   172→            return await _handle_plc(order_id, merchant_id, payload, body)
   173→   173→
   174→   174→        # ── CAN / CANCELLATION_REQUESTED: Cancelamento Solicitado ──
   175→   175→        # iFood exige resposta 202 em ate 5s. Processar em background.
   176→   176→        if event_type in ("CAN", "CANCELLATION_REQUESTED", "cancellationRequested",
   177→   177→                         "CancellationRequested", "cancellation_requested"):
   178→   178→            logger.info("[CANCELLATION_REQUESTED] Pedido %s | Merchant %s | Adicionando ao background", order_id, merchant_id)
   179→   179→            background_tasks.add_task(_handle_can_background, order_id, merchant_id, payload, body)
   180→   180→            return Response(status_code=202, content=b'')
   181→   181→
   182→   182→        # ── CANCELLED: Cancelamento Concluido (por merchant ou cliente) ──
   183→   183→        if event_type in ("CANCELLED", "cancelled", "CANCELLATION_ACCEPTED", "CAR",
   184→   184→                          "CancellationAccepted", "cancellation_accepted"):
   185→   185→            logger.info("[CANCELLED] Pedido %s CANCELADO no iFood - atualizando Odoo", order_id)
   186→   186→            _log_event({
   187→   187→                "level": "info",
   188→   188→                "event_type": "CANCELLED",
   189→   189→                "order_id": order_id,
   190→   190→                "message": "Evento CANCELLED recebido, atualizando Odoo",
   191→   191→            })
   192→   192→            try:
   193→   193→                _update_odoo_status(order_id, "cancelled")
   194→   194→                _update_odoo_state(order_id, "cancel")
   195→   195→                logger.info("[CANCELLED] Pedido %s - Odoo atualizado para cancelled + state=cancel", order_id)
   196→   196→            except Exception as odoo_err:
   197→   197→                logger.error("[CANCELLED] Falha ao atualizar Odoo: %s", odoo_err, exc_info=True)
   198→   198→            return {"status": "ok", "eventType": event_type, "orderId": order_id}
   199→   199→
   200→   200→        # ── CANCELLATION_REQUEST_FAILED: Falha no cancelamento ──
   201→   201→        if event_type in ("CANCELLATION_REQUEST_FAILED", "cancellationRequestFailed",
   202→   202→                          "CancellationRequestFailed", "cancellation_request_failed"):
   203→   203→            logger.warning("[CANCEL_FAILED] Falha no cancelamento do pedido %s no iFood", order_id)
   204→   204→            _log_event({
   205→   205→                "level": "warning",
   206→   206→                "event_type": "CANCELLATION_REQUEST_FAILED",
   207→   207→                "order_id": order_id,
   208→   208→                "message": "Cancelamento falhou no iFood",
   209→   209→            })
   210→   210→            try:
   211→   211→                _update_odoo_status(order_id, "cancellation_failed")
   212→   212→            except Exception as odoo_err:
   213→   213→                logger.error("[CANCEL_FAILED] Falha ao atualizar Odoo: %s", odoo_err, exc_info=True)
   214→   214→            return {"status": "ok", "eventType": event_type, "orderId": order_id}
   215→   215→
   216→   216→        # ── DSP: Despacho ─────────────────────────────────────
   217→   217→        if event_type in ("DSP", "DISPATCHED", "orderDispatched"):
   218→   218→            logger.info("[DSP] Pedido despachado: %s", order_id)
   219→   219→            try:
   220→   220→                _update_odoo_status(order_id, "dispatched")
   221→   221→            except Exception as odoo_err:
   222→   222→                logger.error("[DSP] Falha ao atualizar Odoo: %s", odoo_err, exc_info=True)
   223→   223→            return {"status": "ok", "eventType": event_type, "orderId": order_id}
   224→   224→
   225→   225→        # ── CONCLUDED: Pedido concluido (entrega finalizada) ─────
   226→   226→        if event_type in ("CONCLUDED", "concluded", "DELIVERED", "delivered"):
   227→   227→            logger.info("[CONCLUDED] Pedido %s CONCLUIDO no iFood - atualizando Odoo", order_id)
   228→   228→            try:
   229→   229→                _update_odoo_status(order_id, "concluded")
   230→   230→                # Tambem tentar mudar state para 'done' no Odoo
   231→   231→                _update_odoo_state(order_id, "done")
   232→   232→            except Exception as odoo_err:
   233→   233→                logger.error("[CONCLUDED] Falha ao atualizar Odoo: %s", odoo_err, exc_info=True)
   234→   234→            return {"status": "ok", "eventType": event_type, "orderId": order_id}
   235→   235→
   236→   236→        # ── Status Change generico ────────────────────────────
   237→   237→        if event_type == "orderStatusChanged":
   238→   238→            status = payload.get("newStatus", payload.get("status", ""))
   239→   239→            logger.info("[STATUS_CHANGE] Pedido %s - novo status: %s", order_id, status)
   240→   240→            if status:
   241→   241→                try:
   242→   242→                    _update_odoo_status(order_id, status)
   243→   243→                    # Se o status for concluded/delivered, tambem mudar state
   244→   244→                    if status in ("CONCLUDED", "concluded", "DELIVERED", "delivered"):
   245→   245→                        _update_odoo_state(order_id, "done")
   246→   246→                except Exception as odoo_err:
   247→   247→                    logger.error("[STATUS_CHANGE] Falha ao atualizar Odoo: %s", odoo_err, exc_info=True)
   248→   248→            return {"status": "ok", "eventType": event_type, "orderId": order_id}
   249→   249→
   250→   250→        # ── Evento desconhecido ───────────────────────────────
   251→   251→        logger.info("Evento nao tratado: %s | Payload: %s",
   252→   252→                     event_type, str(payload)[:500])
   253→   253→        _log_event({
   254→   254→            "level": "warning",
   255→   255→            "event_type": event_type,
   256→   256→            "order_id": order_id,
   257→   257→            "message": "Evento nao tratado",
   258→   258→        })
   259→   259→        return {"status": "ok", "eventType": event_type, "orderId": order_id}
   260→   260→
   261→   261→    except Exception as e:
   262→   262→        # CAPTURA QUALQUER ERRO IMPREVISTO - mas SEMPRE retorna 200
   263→   263→        logger.error("[FATAL] Erro NAO TRATADO no webhook iFood: %s", e, exc_info=True)
   264→   264→        _log_event({
   265→   265→            "level": "error",
   266→   266→            "event_type": event_type,
   267→   267→            "order_id": order_id,
   268→   268→            "message": "Erro fatal nao tratado",
   269→   269→            "error": str(e),
   270→   270→        })
   271→   271→        return {"status": "ok", "message": "received"}
   272→   272→
   273→   273→
   274→   274→# ── PLC Handler: Novo Pedido ─────────────────────────────────
   275→   275→
   276→   276→async def _handle_plc(order_id: str, merchant_id: str, payload: dict, body: bytes) -> dict:
   277→   277→    """Processa evento PLC (novo pedido do iFood).
   278→   278→
   279→   279→    Passos independentes:
   280→   280→    1. Buscar dados do pedido e confirmar no iFood
   281→   281→    2. Sincronizar com Odoo
   282→   282→
   283→   283→    Se um passo falha, o outro ainda e tentado.
   284→   284→    """
   285→   285→    logger.info("[PLC] === INICIO FLUXO DE CONFIRMACAO ===")
   286→   286→    logger.info("[PLC] Pedido: %s | Merchant: %s", order_id, merchant_id)
   287→   287→
   288→   288→    ifood_client = IFoodAPIClient(settings)
   289→   289→    order_data = None
   290→   290→    confirmed = False
   291→   291→    synced = False
   292→   292→
   293→   293→    try:
   294→   294→        # ── PASSO 1: Auth + Get Order + Confirm no iFood ─────
   295→   295→        try:
   296→   296→            logger.info("[PLC] Passo 1: Buscando dados do pedido no iFood...")
   297→   297→            async with ifood_client:
   298→   298→                # 1a. Buscar dados completos
   299→   299→                order_data = await ifood_client.get_order(order_id)
   300→   300→                logger.info("[PLC] Dados do pedido obtidos. Campos: %s", list(order_data.keys()) if isinstance(order_data, dict) else type(order_data))
   301→   301→
   302→   302→                # 1b. CONFIRMAR pedido no iFood (obrigatorio para homologacao)
   303→   303→                logger.info("[PLC] Chamando POST /order/v1.0/orders/%s/confirm", order_id)
   304→   304→                confirm_result = await ifood_client.confirm_order(order_id)
   305→   305→                confirmed = True
   306→   306→                logger.info("[PLC] Pedido CONFIRMADO no iFood! Resposta: %s", str(confirm_result)[:500])
   307→   307→
   308→   308→        except Exception as ifood_err:
   309→   309→            logger.error("[PLC] FALHA na comunicacao com iFood: %s", ifood_err, exc_info=True)
   310→   310→            _log_event({
   311→   311→                "level": "error",
   312→   312→                "event_type": "PLC",
   313→   313→                "order_id": order_id,
   314→   314→                "step": "ifood_api",
   315→   315→                "message": "Falha ao buscar/confirmar pedido no iFood",
   316→   316→                "error": str(ifood_err),
   317→   317→            })
   318→   318→
   319→   319→        # ── PASSO 2: Sincronizar com Odoo ────────────────────
   320→   320→        if order_data:
   321→   321→            try:
   322→   322→                logger.info("[PLC] Passo 2: Sincronizando com Odoo...")
   323→   323→                odoo_client = OdooClient(settings)
   324→   324→                sync_service = OdooSyncService(odoo_client, ifood_client)
   325→   325→                sale_order_id = sync_service.sync_order(order_data)
   326→   326→                synced = True
   327→   327→                logger.info("[PLC] Pedido sincronizado -> Odoo sale.order %s", sale_order_id)
   328→   328→
   329→   329→                # Atualizar status no Odoo
   330→   330→                sync_service.update_order_status(order_id, "confirmed")
   331→   331→                logger.info("[PLC] Status atualizado para 'confirmed' no Odoo")
   332→   332→
   333→   333→            except Exception as odoo_err:
   334→   334→                logger.error("[PLC] FALHA na sincronizacao com Odoo: %s", odoo_err, exc_info=True)
   335→   335→                _log_event({
   336→   336→                    "level": "error",
   337→   337→                    "event_type": "PLC",
   338→   338→                    "order_id": order_id,
   339→   339→                    "step": "odoo_sync",
   340→   340→                    "message": "Falha ao sincronizar pedido com Odoo",
   341→   341→                    "error": str(odoo_err),
   342→   342→                })
   343→   343→        else:
   344→   344→            logger.error("[PLC] Nenhum dado de pedido disponivel - pulando sync Odoo")
   345→   345→
   346→   346→        # ── Resumo do processamento ──────────────────────────
   347→   347→        result = {
   348→   348→            "status": "ok",
   349→   349→            "eventType": "PLC",
   350→   350→            "orderId": order_id,
   351→   351→            "confirmed_on_ifood": confirmed,
   352→   352→            "synced_to_odoo": synced,
   353→   353→        }
   354→   354→        logger.info("[PLC] === RESUMO: confirmado=%s | sync_odoo=%s ===",
   355→   355→                     confirmed, synced)
   356→   356→        logger.info("[PLC] === FIM FLUXO DE CONFIRMACAO ===")
   357→   357→        logger.info("=" * 60)
   358→   358→
   359→   359→        _log_event({
   360→   360→            "level": "info",
   361→   361→            "event_type": "PLC",
   362→   362→            "order_id": order_id,
   363→   363→            "confirmed": confirmed,
   364→   364→            "synced": synced,
   365→   365→        })
   366→   366→
   367→   367→        return result
   368→   368→
   369→   369→    finally:
   370→   370→        # Fechar cliente iFood de forma segura
   371→   371→        try:
   372→   372→            await ifood_client.close()
   373→   373→        except Exception:
   374→   374→            pass
   375→   375→
   376→   376→
   377→   377→# ── CAN Handler: Cancelamento Solicitado ─────────────────────
   378→   378→
   379→   379→async def _handle_can_background(order_id: str, merchant_id: str, payload: dict, body: bytes) -> None:
   380→   380→    """Processa evento CANCELLATION_REQUESTED em background (nao bloqueia o webhook response).
   381→   381→
   382→   382→    Fluxo iFood para cancelamento:
   383→   383→    1. Receber CANCELLATION_REQUESTED via webhook
   384→   384→    2. Chamar POST /cancellation/accept (ou /reject) no iFood
   385→   385→    3. Aguardar evento CANCELLED (ou CANCELLATION_REQUEST_FAILED)
   386→   386→    4. Atualizar Odoo com o status final
   387→   387→
   388→   388→    IMPORTANTE: Nao atualizar Odoo para 'cancelled' aqui!
   389→   389→    O status final vem no evento CANCELLED.
   390→   390→    """
   391→   391→    logger.info("[CANCELLATION_REQUESTED] === INICIO FLUXO (background) ===")
   392→   392→    logger.info("[CANCELLATION_REQUESTED] Pedido: %s | Merchant: %s", order_id, merchant_id)
   393→   393→    logger.info("[CANCELLATION_REQUESTED] Payload: %s", body.decode("utf-8", errors="replace")[:3000])
   394→   394→
   395→   395→    cancellation_reason = payload.get("reason", payload.get("cancellationReason", "nao informado"))
   396→   396→    logger.info("[CANCELLATION_REQUESTED] Motivo informado: %s", cancellation_reason)
   397→   397→
   398→   398→    _log_event({
   399→   399→        "level": "info",
   400→   400→        "event_type": "CANCELLATION_REQUESTED",
   401→   401→        "order_id": order_id,
   402→   402→        "merchant_id": merchant_id,
   403→   403→        "message": "Iniciando processamento de cancelamento em background",
   404→   404→    })
   405→   405→
   406→   406→    ifood_client = IFoodAPIClient(settings)
   407→   407→    accepted = False
   408→   408→
   409→   409→    try:
   410→   410→        # ── PASSO 1: Aceitar cancelamento no iFood ───────────
   411→   411→        try:
   412→   412→            logger.info("[CANCELLATION_REQUESTED] Chamando POST /order/v1.0/orders/%s/cancellation/accept", order_id)
   413→   413→            async with ifood_client:
   414→   414→                accept_result = await ifood_client.accept_cancellation(order_id)
   415→   415→            accepted = True
   416→   416→            logger.info("[CANCELLATION_REQUESTED] Cancelamento ACEITO no iFood: %s",
   417→   417→                         str(accept_result)[:500])
   418→   418→            _log_event({
   419→   419→                "level": "info",
   420→   420→                "event_type": "CANCELLATION_REQUESTED",
   421→   421→                "order_id": order_id,
   422→   422→                "step": "accept_ifood",
   423→   423→                "message": "Cancelamento aceito com sucesso no iFood",
   424→   424→                "accepted": True,
   425→   425→            })
   426→   426→        except Exception as ifood_err:
   427→   427→            logger.error("[CANCELLATION_REQUESTED] FALHA ao aceitar cancelamento no iFood: %s",
   428→   428→                         ifood_err, exc_info=True)
   429→   429→            _log_event({
   430→   430→                "level": "error",
   431→   431→                "event_type": "CANCELLATION_REQUESTED",
   432→   432→                "order_id": order_id,
   433→   433→                "step": "accept_ifood",
   434→   434→                "message": "Falha ao aceitar cancelamento no iFood",
   435→   435→                "error": str(ifood_err),
   436→   436→                "accepted": False,
   437→   437→            })
   438→   438→
   439→   439→        # ── PASSO 2: Atualizar Odoo para 'cancellation_requested' ──
   440→   440→        # NAO colocar 'cancelled' ainda! Aguardar o evento CANCELLED.
   441→   441→        try:
   442→   442→            sync_status = "cancellation_accepted" if accepted else "cancellation_requested"
   443→   443→            _update_odoo_status(order_id, sync_status)
   444→   444→            logger.info("[CANCELLATION_REQUESTED] Odoo atualizado para '%s' (aguardando evento CANCELLED)", sync_status)
   445→   445→        except Exception as odoo_err:
   446→   446→            logger.error("[CANCELLATION_REQUESTED] Falha ao atualizar Odoo: %s", odoo_err, exc_info=True)
   447→   447→
   448→   448→        logger.info("[CANCELLATION_REQUESTED] === RESUMO: aceito=%s ===", accepted)
   449→   449→        logger.info("[CANCELLATION_REQUESTED] === FIM PROCESSAMENTO (aguardando CANCELLED) ===")
   450→   450→
   451→   451→    finally:
   452→   452→        try:
   453→   453→            await ifood_client.close()
   454→   454→        except Exception:
   455→   455→            pass
   456→   456→
   457→   457→
   458→   458→# ── Helper: Atualizar status no Odoo ─────────────────────────
   459→   459→
   460→   460→def _update_odoo_status(ifood_order_id: str, status: str) -> bool:
   461→   461→    """Atualiza o campo x_studio_ifood_status no sale.order do Odoo."""
   462→   462→    try:
   463→   463→        odoo_client = OdooClient(settings)
   464→   464→        sync_service = OdooSyncService(odoo_client, IFoodAPIClient(settings))
   465→   465→        return sync_service.update_order_status(ifood_order_id, status)
   466→   466→    except Exception as e:
   467→   467→        logger.error("[ODOO] Falha ao atualizar status %s para pedido %s: %s",
   468→   468→                     status, ifood_order_id, e, exc_info=True)
   469→   469→        raise
   470→   470→
   471→   471→
   472→   472→def _update_odoo_state(ifood_order_id: str, state: str) -> None:
   473→   473→    """Muda o state do sale.order no Odoo (ex: 'done', 'cancel')."""
   474→   474→    try:
   475→   475→        odoo_client = OdooClient(settings)
   476→   476→        # Buscar o ID do sale.order pelo x_studio_ifood_order_id
   477→   477→        orders = odoo_client.search_read(
   478→   478→            "sale.order",
   479→   479→            domain=[("x_studio_ifood_order_id", "=", ifood_order_id)],
   480→   480→            fields=["id", "state"],
   481→   481→        )
   482→   482→        if not orders:
   483→   483→            logger.warning("[ODOO] Pedido %s nao encontrado no Odoo para mudar state", ifood_order_id)
   484→   484→            return
   485→   485→        for order in orders:
   486→   486→            if order.get("state") == state:
   487→   487→                continue
   488→   488→            odoo_client.execute_kw(
   489→   489→                "sale.order",
   490→   490→                "write",
   491→   491→                [[order["id"]], {"state": state}],
   492→   492→            )
   493→   493→            logger.info("[ODOO] Pedido %s (Odoo %d) -> state='%s'", ifood_order_id, order["id"], state)
   494→   494→    except Exception as e:
   495→   495→        logger.error("[ODOO] Falha ao mudar state '%s' para pedido %s: %s",
   496→   496→                     state, ifood_order_id, e, exc_info=True)
   497→   497→
   498→   498→
   499→   499→# ── Polling: Cancelamentos Odoo → iFood ────────────────────
   500→   500→
   501→   501→async def _check_odoo_pending_cancellations() -> dict:
   502→   502→    """Verifica pedidos cancelados no Odoo que ainda nao foram cancelados no iFood.
   503→   503→
   504→   504→    Busca sale.order onde:
   505→   505→      - state = 'cancel' (cancelado no Odoo)
   506→   506→      - x_studio_ifood_order_id esta preenchido
   507→   507→      - x_studio_ifood_status != 'cancelled' (ainda nao propagado)
   508→   508→
   509→   509→    Para cada pedido encontrado, envia requestCancellation ao iFood
   510→   510→    e marca status como 'cancellation_requested' no Odoo.
   511→   511→    O cancelamento final acontece quando o iFood envia o evento CANCELLED.
   512→   512→    """
   513→   513→    logger.info("[ODOO_POLL] Verificando cancelamentos pendentes no Odoo...")
   514→   514→
   515→   515→    odoo_client = OdooClient(settings)
   516→   516→    results = {"checked": 0, "requested": 0, "errors": []}
   517→   517→
   518→   518→    try:
   519→   519→        orders = odoo_client.search_read(
   520→   520→            "sale.order",
   521→   521→            domain=[
   522→   522→                ("state", "=", "cancel"),
   523→   523→                ("x_studio_ifood_order_id", "!=", False),
   524→   524→            ],
   525→   525→            fields=[
   526→   526→                "id", "x_studio_ifood_order_id",
   527→   527→                "x_studio_ifood_cancel_reason",
   528→   528→                "x_studio_ifood_status",
   529→   529→            ],
   530→   530→        )
   531→   531→    except Exception as e:
   532→   532→        logger.error("[ODOO_POLL] Falha ao buscar pedidos no Odoo: %s", e, exc_info=True)
   533→   533→        return results
   534→   534→
   535→   535→    results["checked"] = len(orders)
   536→   536→    if not orders:
   537→   537→        logger.info("[ODOO_POLL] Nenhum cancelamento pendente encontrado")
   538→   538→        return results
   539→   539→
   540→   540→    logger.info("[ODOO_POLL] Encontrados %d pedidos cancelados no Odoo", len(orders))
   541→   541→
   542→   542→    async with IFoodAPIClient(settings) as ifood_client:
   543→   543→        for order in orders:
   544→   544→            ifood_id = str(order.get("x_studio_ifood_order_id", ""))
   545→   545→            ifood_status = str(order.get("x_studio_ifood_status", ""))
   546→   546→
   547→   547→            if not ifood_id:
   548→   548→                continue
   549→   549→
   550→   550→            # Ja foi cancelado ou ja tem solicitacao em andamento?
   551→   551→            if ifood_status in ("cancelled", "cancellation_requested"):
   552→   552→                continue
   553→   553→
   554→   554→            reason_code = str(order.get("x_studio_ifood_cancel_reason", ""))
   555→   555→            # Extrair apenas o codigo numerico (ex: "506:Endereco..." -> "506")
   556→   556→            if ":" in reason_code:
   557→   557→                reason_code = reason_code.split(":")[0].strip()
   558→   558→            if not reason_code or not reason_code.isdigit():
   559→   559→                reason_code = "501"  # default: Erro no sistema
   560→   560→
   561→   561→            logger.info("[ODOO_POLL] Solicitando cancelamento pedido %s no iFood (motivo: %s)...",
   562→   562→                         ifood_id, reason_code)
   563→   563→
   564→   564→            try:
   565→   565→                await ifood_client.merchant_request_cancellation(ifood_id, reason_code=reason_code)
   566→   566→                results["requested"] += 1
   567→   567→
   568→   568→                # Marcar como 'cancellation_requested' no Odoo (nao 'cancelled' ainda!)
   569→   569→                try:
   570→   570→                    sync_service = OdooSyncService(odoo_client, ifood_client)
   571→   571→                    sync_service.update_order_status(ifood_id, "cancellation_requested")
   572→   572→                except Exception:
   573→   573→                    pass
   574→   574→
   575→   575→                logger.info("[ODOO_POLL] Solicitacao de cancelamento ENVIADA para pedido %s - aguardando evento CANCELLED do iFood", ifood_id)
   576→   576→
   577→   577→            except Exception as e:
   578→   578→                logger.error("[ODOO_POLL] Falha ao solicitar cancelamento pedido %s: %s",
   579→   579→                             ifood_id, e, exc_info=True)
   580→   580→                results["errors"].append(f"{ifood_id}: {str(e)}")
   581→   581→
   582→   582→    logger.info("[ODOO_POLL] Resultado: verificados=%d, solicitados=%d",
   583→   583→                 results["checked"], results["requested"])
   584→   584→    return results
   585→   585→
   586→   586→
   587→   587→# ── Polling: Despachos Odoo → iFood ──────────────────────────
   588→   588→
   589→   589→async def _check_odoo_pending_dispatches() -> dict:
   590→   590→    """Verifica pedidos confirmados no Odoo que ainda nao foram despachados no iFood.
   591→   591→
   592→   592→    Busca sale.order onde:
   593→   593→      - state = 'sale' (confirmado via action_confirm no Odoo)
   594→   594→      - x_studio_ifood_order_id esta preenchido
   595→   595→      - x_studio_ifood_status != 'dispatched' e != 'cancelled'
   596→   596→
   597→   597→    Para cada pedido encontrado, chama dispatch no iFood.
   598→   598→    """
   599→   599→    logger.info("[ODOO_DISPATCH] Verificando despachos pendentes no Odoo...")
   600→   600→
   601→   601→    odoo_client = OdooClient(settings)
   602→   602→    results = {"checked": 0, "dispatched": 0, "errors": []}
   603→   603→
   604→   604→    try:
   605→   605→        orders = odoo_client.search_read(
   606→   606→            "sale.order",
   607→   607→            domain=[
   608→   608→                ("state", "=", "sale"),
   609→   609→                ("x_studio_ifood_order_id", "!=", False),
   610→   610→            ],
   611→   611→            fields=[
   612→   612→                "id", "x_studio_ifood_order_id",
   613→   613→                "x_studio_ifood_status",
   614→   614→            ],
   615→   615→        )
   616→   616→    except Exception as e:
   617→   617→        logger.error("[ODOO_DISPATCH] Falha ao buscar pedidos no Odoo: %s", e, exc_info=True)
   618→   618→        return results
   619→   619→
   620→   620→    results["checked"] = len(orders)
   621→   621→    if not orders:
   622→   622→        logger.info("[ODOO_DISPATCH] Nenhum despacho pendente encontrado")
   623→   623→        return results
   624→   624→
   625→   625→    async with IFoodAPIClient(settings) as ifood_client:
   626→   626→        for order in orders:
   627→   627→            ifood_id = str(order.get("x_studio_ifood_order_id", ""))
   628→   628→            ifood_status = str(order.get("x_studio_ifood_status", ""))
   629→   629→
   630→   630→            if not ifood_id:
   631→   631→                continue
   632→   632→
   633→   633→            # Ja foi despachado ou cancelado?
   634→   634→            if ifood_status in ("dispatched", "cancelled", "cancellation_requested", "delivered", "concluded"):
   635→   635→                continue
   636→   636→
   637→   637→            logger.info("[ODOO_DISPATCH] Despachando pedido %s no iFood (status atual: %s)...",
   638→   638→                         ifood_id, ifood_status)
   639→   639→
   640→   640→            try:
   641→   641→                await ifood_client.dispatch_order(ifood_id)
   642→   642→                results["dispatched"] += 1
   643→   643→
   644→   644→                # Atualizar status no Odoo
   645→   645→                try:
   646→   646→                    sync_service = OdooSyncService(odoo_client, ifood_client)
   647→   647→                    sync_service.update_order_status(ifood_id, "dispatched")
   648→   648→                except Exception:
   649→   649→                    pass
   650→   650→
   651→   651→                logger.info("[ODOO_DISPATCH] Pedido %s DESPACHADO no iFood com sucesso!", ifood_id)
   652→   652→
   653→   653→            except Exception as e:
   654→   654→                logger.error("[ODOO_DISPATCH] Falha ao despachar pedido %s: %s",
   655→   655→                             ifood_id, e, exc_info=True)
   656→   656→                results["errors"].append(f"{ifood_id}: {str(e)}")
   657→   657→
   658→   658→    if results["dispatched"] > 0:
   659→   659→        logger.info("[ODOO_DISPATCH] Resultado: verificados=%d, despachados=%d",
   660→   660→                     results["checked"], results["dispatched"])
   661→   661→    return results
   662→   662→
   663→   663→
   664→   664→# ── Catalog webhook ──────────────────────────────────────────
   665→   665→
   666→   666→@router.post("/webhooks/ifood/catalog")
   667→   667→@router.get("/webhooks/ifood/catalog")
   668→   668→async def handle_catalog_webhook(request: Request):
   669→   669→    """Recebe eventos de catalogo do iFood."""
   670→   670→    try:
   671→   671→        body = await request.body()
   672→   672→        logger.info("Webhook catalogo iFood recebido (method: %s, body_len: %d)",
   673→   673→                     request.method, len(body))
   674→   674→        logger.debug("Body RAW: %s", body.decode("utf-8", errors="replace")[:3000])
   675→   675→        return {"status": "ok", "message": "received"}
   676→   676→
   677→   677→    except Exception as e:
   678→   678→        logger.error("Erro no webhook catalogo: %s", e, exc_info=True)
   679→   679→        return {"status": "ok", "message": "received"}